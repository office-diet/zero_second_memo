<div class="today">
  <div class="form-head">
  </div>
  <% memos_tags.each do |memo| %>
    <div class="today-theme"><%= link_to memo.theme, memo_path(memo.id) %></h2></div>
    <div class="today-memo"><%= simple_format(memo.text) %></div>
    <hr>
    <div class="today-tags">
      <% memo.tags.each do |tag| %>
        <div class="today-tag"><%=link_to tag.name, tag_memo_path(tag.id) %></div>
      <% end %>
    </div>
    <hr>
  <% end %>
</div>