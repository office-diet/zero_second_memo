<div class="memo-body">
  <%= render partial: "memos/side", locals: { side_memos: @side_memos, tags: @tags } %>
  
  <div class="memo">
    <div class="form-head">
      <%= "#{@date}のゼロ秒メモ" %>
    </div>
    <div class="show">  
      <%= @memo.theme %>
      <%= simple_format(@memo.text) %>
      <hr>
      <div class="list-tags">
        <% @memo.tags.each do |tag| %>
          <div class="list-tag"><%=link_to tag.name, tag_memo_path(@memo.id, tag: tag.id) %></div>
        <% end %>
      </div>
      <hr>
    </div>
  </div>
  
  <%= render partial: "memos/list", locals: { list_memos: @list_memos, date: @date } %>
</div>